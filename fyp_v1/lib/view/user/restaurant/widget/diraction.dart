import 'package:flutter/material.dart';

class DiractionPage extends StatelessWidget {
  const DiractionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Text('loactoipn'),
    );
  }
}
